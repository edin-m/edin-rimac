export type GameMove = {
    playerId: string,
    gameId: string,
    row: number,
    col: number,
    val: string
};

export type GameId = string;

export type PlayerId = string;

export type GameType = string;
const services = {
    GAME_BOARD_SIZE: 3,
    GAME_MODEL: 'GAME_MODEL',
    GAME_SERVICE: 'GAME_SERVICE',
}

const events = {
    GAME_EVENT: 'GAME_EVENT',
};

const gameTypes = {
    sp: 'sp',
    mp: 'mp'
}

const GAME_BOARD_SIZE = 3;

export { 
    events,
    services,
    gameTypes,
    GAME_BOARD_SIZE
};

